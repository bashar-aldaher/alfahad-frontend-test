const config = {
  //API ENDPOINTS
  ADS_ENDPOINT: "ads",
  PRODUCTS_ENDPOINT: "products",

  //CONSTANT
  API_KEY: "",

  API_URL: "https://testing.rentchicken.net/api",
};

const environment = {
  ...config,
};

export default environment;
